[package]
name = "roster-cjk"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["308K <@308K>"]
license = "MPL-2.0"
description = "Lay out lists of CJK (Chinese / Japanese) names into a tidy, column-aligned roster grid."
repository = "https://github.com/308K/roster-cjk"
keywords = ["names", "roster", "chinese", "japanese", "cjk", "ruby", "layout", "i18n"]
categories = ["components", "languages", "layout"]
disciplines = ["linguistics"]
compiler = "0.14.0"

[tool.typst]
deprecated = false
